namespace StreamedGeometries.index;

table StreamedGeometries {
    geometries: [StreamedGeometry];
}

table StreamedGeometry {
    id: string;
    position: [float];
    normal: [float];
    index: [uint];
}

root_type StreamedGeometries;